import Login from "../components/Forms/Login"

const LoginForm = () => {
  return (
    <div>
        <header>
            <h1>Please log in</h1>
        </header>
        <main>
          < Login />
        </main>
    </div>
  )
}

export default LoginForm
