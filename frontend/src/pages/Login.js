import Login from "../components/Buttons/Login"

const Login = () => {
  return (
    <div>
        <header>
            <h1>Please log in</h1>
        </header>
        <main>
          < Login />
        </main>
    </div>
  )
}

export default Login
